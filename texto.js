const Linkedin = [
    { Frontend: [{}, {}, {}] },
    { Backend: [{}, {}, {}] },
    {
        Portafolio: (smanzano) => {
            let https = smanzano.dev;
            return https;
        },
    },
];

// Desarrollador Web Full Stack

const Frontend = [{ HTML }, { CSS }, { JavaScript }, { ReactJs }];
const Backend = [{ NodeJs }, { Express }, { MySql }, { CloudFlare }];
function Portafolio(smanzano) {
    let https = smanzano.dev;
    return https;
}
